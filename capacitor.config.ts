import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.motanamy.suhba',
  appName: 'صحبة القران',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
